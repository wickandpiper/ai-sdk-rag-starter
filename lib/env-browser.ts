// This is a browser-safe version of the environment variables
// It only includes variables that are safe to expose to the client

export const env = {
  // Add any environment variables that are safe to expose to the client
  // For example:
  // PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || '',
}; 